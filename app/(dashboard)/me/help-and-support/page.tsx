import HelpSupportLayout from "@/src/layouts/dashboard/account/help-support";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Growe | Help & Support",
};

const HelpSupportPage = () => {
  return <HelpSupportLayout />;
};

export default HelpSupportPage;
