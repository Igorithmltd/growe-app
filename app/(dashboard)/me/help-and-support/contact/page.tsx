import ContactSupportLayout from "@/src/layouts/dashboard/account/help-support/contact";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Growe | Contact Support",
};

const ContactSupportPage = () => {
  return <ContactSupportLayout />;
};

export default ContactSupportPage;
